name: minerva_controller_annotation
description: Annotations for code generation with package `minerva_controller_generator`.
version: 1.0.0
homepage: https://github.com/GlebBatykov/minerva_controller_annotation

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  minerva: ^0.3.2

dev_dependencies:
  lints: ^2.0.0
